# 项目 · ECC 能力选型

> 来源：[affaan-m/ECC](https://github.com/affaan-m/ECC)（约 278 skills / 66 agents）  
> **DAILY** = 已 vendoring 到规范库根目录；部署到业务项目时放入 `.cursor/`。**LIBRARY** = 按需，勿整仓拷贝。

---

## DAILY（已落地到规范库根目录）

部署到业务项目：复制到 `.cursor/` 下同名路径。

### Skills（本库 + vendored）

| 类别 | 代表 |
|------|------|
| 工作流 | `scope-check`、`requirements-refinement`、`implement-feature`、`workflow-playbooks`… |
| 产品/调研/设计 | `market-research`、`deep-research`、`product-capability`、`blueprint`、`frontend-design-direction`、`ui-to-vue`、`make-interfaces-feel-better` |
| 浏览器 / E2E | `agent-browser` |
| UI 设计（第三方） | `frontend-design`、`ui-ux-pro-max`、`web-design-guidelines` |
| **P0 React PC** | `frontend-patterns`、`react-performance`、`frontend-a11y`、`nextjs-turbopack` |
| **P0 React App** | `react-native-patterns` |
| **P1 大型项目** | `orch-*`、`plan-orchestrate`、`team-agent-orchestration`、`architecture-decision-records`、`agent-architecture-audit`、`hexagonal-architecture`、`parallel-execution-optimizer`、`autonomous-loops`、`continuous-agent-loop`、`production-audit` |
| **P2** | `cost-aware-llm-pipeline`、`cost-tracking`、`documentation-lookup`、`api-design`、`backend-patterns`、`strategic-compact`、`continuous-learning-v2` |

### Rules

| 类别 | 文件 |
|------|------|
| 工作流 | `workflow-triggers.mdc`、`ai-execution.mdc`、`project-core.mdc` |
| 项目栈 | `backend-spring.mdc`、`frontend-react.mdc`、`frontend-vue.mdc`、`frontend-react-native.mdc`、`api-contracts.mdc`、`docs-maintenance.mdc` |
| ECC common / java / react / typescript / vue | `common-*`、`java-*`、`react-*`、`typescript-*`、`vue-*` |
| **P0 RN** | `react-native-*.mdc`（8 条） |
| **P0 Web** | `web-*.mdc`（7 条；RN 项目勿与 DOM 假设混用） |

### Agents

| 类别 | 代表 |
|------|------|
| ECC | `java-reviewer`、`react-reviewer`、`security-reviewer`、`architect`、`planner`、`a11y-architect`… |
| Vue | `vue-reviewer`、`vue-build-resolver`、`frontend-vue-dev` |
| React Native | `frontend-rn-dev` |
| 项目 | `product-manager`、`backend-dev`、`frontend-dev`、`qa-engineer`、`doc-sync` |

### Evals / Hooks

| 项 | 说明 |
|----|------|
| `evals/_example-*.md` | EDD 示例 |
| `hooks` stop | DoD 提醒 |

---

## LIBRARY（刻意不整包引入）

| 类别 | 原因 |
|------|------|
| 全量 278 skills | 噪声大；按栈选择性 sync |
| 全量 ECC hooks | 依赖插件 node 脚本 |
| django / go / rust / kotlin 等栈 | 非本库默认 DAILY；需要时从 ECC 拉 |

---

## 维护

```powershell
# 从 ~/.cursor 刷新 agents + 已列 skills
powershell -File scripts/sync-ecc-bundle.ps1

# 从 GitHub 稀疏拉取 skills（-FromGitHub）
powershell -File scripts/sync-ecc-bundle.ps1 -FromGitHub

# 刷新 react-native / web 规则（paths→globs）
powershell -File scripts/sync-ecc-github-rules.ps1

# 第三方 skills CLI
powershell -File scripts/sync-vendor-skills.ps1
```

项目约束写在 `agents/*-dev.md` / `constraints.md`，勿被 sync 覆盖。
