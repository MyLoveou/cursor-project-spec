# 代码审查

> 状态：**已定稿** · {DATE}  
> 关联：`.cursor/skills/code-review-gate/SKILL.md` · `.cursor/rules/common-code-review.mdc`

## 1. 目的与时机

代码审查用于在合并或交付前发现质量、安全与规格偏差。本项目在以下场景**必须**审查：

| 场景 | 说明 |
|------|------|
| 功能改动完成 | Agent 或开发者交付前 |
| 用户明确要求 | 「审查」「review」「合并前看看」 |
| 跨栈 / 持久化 / IPC | 须组合 domain + 主进程 + 前端审查 |
| 安全敏感 | auth、用户输入边界、文件路径、密钥 |

审查通过后，再进入 **Bugbot**（可选/PR 前推荐）与 `verification-gate`（构建、测试、手测）。

完整流水线见 [工作流索引.md](./工作流索引.md)。

## 2. 委派与工具

| 改动范围 | 审查 Agent |
|----------|------------|
| `{FRONTEND_DIR}/**/*.{vue,tsx}` · 编辑器 | `code-reviewer` + `vue-reviewer` |
| `{BACKEND_DIR}/**` | `code-reviewer` + `typescript-reviewer` |
| `{DOMAIN_PKG}/**` · `{DB_PKG}/**` | `code-reviewer` |
| 安全 / 权限 | `security-reviewer` |
| 数据库 migration | `database-reviewer` |
| diff 回归 / 静默失败 | `/review-bugbot` → [Bugbot审查.md](./Bugbot审查.md) |
| 新功能 / 修 bug 测试 | `/tdd-workflow` → [TDD与测试.md](./TDD与测试.md) |

## 3. 严重度与裁决

| 级别 | 含义 | 动作 |
|------|------|------|
| **CRITICAL** | 安全漏洞、数据丢失、密钥泄露 | **BLOCK** — 必须修复 |
| **HIGH** | 功能错误、规格明确违背、编辑/保存丢失 | **WARN** — 合并/交付前应修复 |
| **MEDIUM** | 可维护性、错误码、测试缺口 | **INFO** — 建议修复 |
| **LOW** | 样式细节、文档滞后 | **NOTE** — 可选 |
| **INFO** | 计划内未实现（Phase 边界） | 记入 backlog，不阻塞 Phase 内交付 |

**裁决：**

- **Approve**：无 CRITICAL / HIGH
- **Warning**：仅有 HIGH（可谨慎合并，须跟踪修复）
- **Block**：存在 CRITICAL

## 4. 通用必查项

- [ ] 符合 `docs/requirements/features/<能力>/需求.md` 与已定稿 L3 方案
- [ ] IPC / 实体与 `docs/design/API设计.md` 一致
- [ ] 渲染进程不经 preload 直连 DB；domain 零 Electron 依赖
- [ ] 无密钥、`.env`、`{LOCAL_DB_PATH}` 误提交
- [ ] 不可变数据：store / patch 不原地 mutate
- [ ] 边界输入校验（主进程 / domain）
- [ ] 新能力有对应测试；domain 行覆盖 ≥ 80%
- [ ] 用户可见错误有 toast + 可理解文案（非裸 stack）
- [ ] 改 `app-services` / repo / save·search 路径 → [performance.md](./performance.md) §4（无 N+1、无 triple read）
- [ ] 改 domain flatten/diff → `{BENCH_CMD}` 无新增 fail

## 5. 领域规范（按模块）

| 模块 | 规范文档 |
|------|----------|
| 块编辑器 · TipTap · 纸面 CSS · savePatch | [{DOMAIN_SPEC}.md](./{DOMAIN_SPEC}.md) |
| savePatch · FTS · search · IPC · embed | [performance.md](./performance.md) |
| 目录树 · 拖拽 · 重命名 · 选中 | [{DOMAIN_SPEC}.md](./{DOMAIN_SPEC}.md) |
| 版本与 Phase 边界 | [版本与动工门禁.md](./版本与动工门禁.md) |
| UI 视觉与布局 | [../UI/界面规格.md](../UI/界面规格.md) |
| 树 / 标题 / 切换 loading | [../功能/v1.0-笔记创作/UI交互补遗.md](../功能/v1.0-笔记创作/UI交互补遗.md) |

## 6. 审查输出模板

```markdown
## 审查结果 · <能力/模块> · <日期>

**裁决：** Approve / Warning / Block

### 摘要
（2–3 句）

### 发现
| 严重度 | 文件 | 问题 | 建议 |
|--------|------|------|------|

### 待修复（HIGH+）
- [ ] …

### 是否可进入 verification-gate
是 / 否
```

审查记录归档：写入对应领域规范（如 `{DOMAIN_SPEC}.md` §审查记录）或 PR 描述。

## 7. 与 Bugbot、verification-gate 分工

| 环节 | 职责 |
|------|------|
| [TDD与测试.md](./TDD与测试.md) | RED→GREEN→REFACTOR；覆盖率 ≥ 80% |
| code-review-gate（本文） | 质量、规格、安全 |
| [Bugbot审查.md](./Bugbot审查.md) | diff 回归、静默 bug |
| verification-gate | `{LINT_CMD}` · `test` · `build` · `dev` 手测 |

**推荐顺序：** TDD → code-review → Bugbot → verification-gate

有 BLOCKER → 先修再进入下一环节。
