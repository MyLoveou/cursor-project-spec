# 开发工作流索引

> 状态：**已定稿** · {DATE}  
> 说明：Agent / 开发者交付代码时的**标准流水线**与规范文档映射。

## 流水线总览

```
需求定稿 → L3 方案定稿 → TDD 实现 → code-review-gate → Bugbot → verification-gate → 交付
```

| 阶段 | Skill / 命令 | 规范文档 |
|------|--------------|----------|
| 范围与需求 | `scope-check` · `requirements-refinement` | [版本与动工门禁.md](./版本与动工门禁.md) |
| L3 方案 | `plan-workflow` | `docs/design/features/<id>-plan.md` |
| 实现 | `implement-feature` | `docs/requirements/features/<id>.md` |
| **TDD** | `/tdd-workflow` · `@tdd-guide` | [TDD与测试.md](./TDD与测试.md) |
| **代码审查** | `code-review-gate` · `code-reviewer` | [代码审查.md](./代码审查.md) |
| **Bugbot** | `/review-bugbot` | [Bugbot审查.md](./Bugbot审查.md) |
| 领域专项（可选） | — | `docs/standards/{DOMAIN_SPEC}.md` |
| 交付门禁 | `verification-gate` | `.cursor/skills/verification-gate/SKILL.md` |

## 何时用哪条工作流

| 用户/场景信号 | 入口 |
|---------------|------|
| 新功能、新 API | TDD → implement-feature → code-review → Bugbot → verification-gate |
| 修 bug、小改 | TDD（可简化 RED）→ code-review → Bugbot（`uncommitted changes`） |
| 「审查」「review」 | [代码审查.md](./代码审查.md) |
| `/review-bugbot` | [Bugbot审查.md](./Bugbot审查.md) |
| `/tdd-workflow`、写测试 | [TDD与测试.md](./TDD与测试.md) |

## 串联优先级（与 workflow-triggers 一致）

`scope-check` → 需求定稿 → `plan-workflow`（L3 定稿）→ **TDD** → `implement-feature` → **code-review-gate** → **Bugbot** → **verification-gate**

## 裁决组合

| 环节 | Block 条件 |
|------|------------|
| TDD | 无 RED 证据即改生产代码；覆盖率 < {MIN_COVERAGE}%（新能力） |
| code-review-gate | CRITICAL；或未经修复的 HIGH |
| Bugbot | CRITICAL / HIGH 未确认或修复 |
| verification-gate | `{LINT_CMD}` · `{TEST_CMD}` · `{BUILD_CMD}` 失败；后端改动未重启验证 |

**说明：** Bugbot 与 `code-reviewer` 互补——前者偏 diff 回归与静默失败；后者偏架构与规格。
