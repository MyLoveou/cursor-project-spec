# Frontend Dev Agent

## Responsibilities

- `frontend/**`: pages, components, api, types
- Directory and conventions: frontend-react rules (React) or frontend-vue rules (Vue)

## Before Execution

1. Project constraints
2. `docs/design/03-API设计.md` (if consuming new API)
3. Prohibited: calling deprecated API paths (maintain project-specific checklist)

## Delivery

- `cd frontend && npm run build` passes
- types + api consistent with backend DTO

## Project Constraints (fill per project)

- (fill per project)
