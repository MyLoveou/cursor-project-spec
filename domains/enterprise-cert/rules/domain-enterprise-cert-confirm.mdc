---
description: >-
  企业认证知识发现：业务规则、领域知识、UI 交互、站点差异、回填校验等凡不确定或表中未载明的，
  须开放式识别并上报待决断，禁止用固定清单代替穷尽发现；决断后写入知识表。
globs: "**/enterprise-cert/**,**/enterprise/**/cert*/**,**/*EnterpriseCert*,**/*[Kk][Yy][Bb]*,**/*[Bb]ackfill*,**/setting-center/**/*[Cc]ert*,**/docs/**/*cert*,**/docs/**/*kyb*,**/docs/**/*认证*"
alwaysApply: false
---

# 企业认证 · 开放式知识发现与上报

> **表是结果，不是边界。** `taboos.md` / `glossary.md` 等只是已沉淀知识；  
> 真实业务远多于表中条目。AI 必须**主动发现**新规则/术语/交互，上报人决断，再**回写入库**。  
> 详流程：Skill `domain-enterprise-cert-knowledge-capture`。

## 强制原则

1. **开放发现**：不依赖、不限于既有 T#/Q# 或任何固定信号表；表外场景同样要找出来
2. **不确定即上报**：业务含义、可否兜底、交互时机、站点差异等未在已确认表中写明 → 上报，禁止猜
3. **决断后入库**：人给出结论后，写入对应知识表（见下），下次直接遵循，勿只留在聊天
4. **已入库可执行**：仅「已确认」条目可作为实现依据

## 何时扫描（开放维度）

对用户话、PRD、Figma、方案、代码 diff，按维度**穷尽提问式扫描**（有则记，无则跳过该维）：

| 维度 | 发现什么（示例，非封闭列表） |
|------|------------------------------|
| 业务规则 | 校验/拦截/优先级/状态迁移/权限联动/渠道条件… |
| 领域知识 | 术语、主体类型、国家/站点语义、渠道能力边界… |
| UI 交互 | 进页/离页/下一步/回退、回填展示、提示、空态、AB 下差异… |
| 数据与时机 | 何时拉 KYB、草稿/快照关系、对比规则… |
| 差异 | 站点 × 国家 × 渠道 × AB 是否行为不同… |
| 实现取舍 | 改旧/新增、配置化、是否新实体… |

既有 `taboos`/`glossary` 中的条目是**种子**，用于对照「是否已确认」；**未命中种子 ≠ 无需确认**。

## 动作

```text
发现候选知识 → 对照知识表是否已有「已确认」结论
  → 无结论：输出【知识待决断】→ STOP（或暂停该点实现）
  → 人决断后：执行 knowledge-capture 回写对应表 → 再继续
```

上报格式与回写目标以 Skill `domain-enterprise-cert-knowledge-capture` 为准。

## 禁止

- 只用固定信号表扫描，遗漏表外规则/交互
- 决断只留在聊天、不写回知识表
- 把「AI 以为合理」当成已确认业务规则
