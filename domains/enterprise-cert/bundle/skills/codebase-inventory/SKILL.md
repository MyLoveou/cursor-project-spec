---
name: codebase-inventory
description: >-
origin: ECC
  代码现状多轮摸底与证据门禁。触发：现状摸底、代码梳理、inventory、codebase map�?
  写方案前摸底、防漏梳。强制分轮检索、路径级证据、对抗复核；AI 初稿≠已核实�?
---

# 代码现状摸底（Codebase Inventory�?

> **目标**：写技术方案前，用可复核证据描述「现在有什么」，而不是凭印象填表�? 
> **硬事�?*：一�?explore �?摸底完成；空白单元格 ≠「各地相同」�?

## 何时�?

- 大需�?/ SDD 写方案前
- 用户说「先梳代码」「现状摸底」「有哪些可复用�?
- 域包 `*-codebase-map` 委托�?Skill 做通用流程

## 产出

写入约定路径（默�?`docs/design/features/<id>-codebase-map.md`，或域模板指定路径），且满足下方**证据与门�?*�?

---

## 强制流程（四轮，不可合并成一轮聊天了事）

### �?0 · 范围锚定

1. 读已定稿/疑问表关闭后的需求要点（�?`requirements-gap`�?
2. 列出**检索关键词清单**（功能名、渠道名、站�?国家、AB、表单字段、接口词�?
3. 列出**对比�?*（至少：站点 × 国家/地区 × 渠道；按需求裁剪，但须显式写出「本需求不比什么」）

输出：`## 检索计划`（关键词 + 对比�?+ 计划 Grep/Glob 模式�?

### �?1 · 入口与主路径（委派）

**优先�?*（有则用，无则降级）�?

1. **CodeGraph MCP**（若已配置）：`codegraph_explore`（或当前版主工具）查入口、调用链、影响面 �?见域�?`tooling/codegraph.md`
2. Task `explore` **�?* `@code-explorer`（只读）
3. Grep / Glob 补洞

要求产出：入�?�?调用�?�?关键文件表（路径 + 符号�? 
将结果贴进摸底文档「主路径」节�?*保留文件路径**（图谱结果也必须�?md，不能只留在工具输出里）

### �?2 · 按对比轴差分（本轮最易漏，必做）

对每一对比轴组合（例：大陆 vs 香港）分别检索：

- �?CodeGraph：对两侧关键�?符号分别 explore，再核对是否同一调用�?
- �?CodeGraph：Grep/读代�?

| 检�?| 不合格表�?|
|------|------------|
| API / client / URL / service �?| 只写「认证接口」无路径 |
| 配置�?/ 枚举 / if 站点分支 | 断言「一样」但无两侧证�?|
| 组件 / hook / 校验 | 只描述一�?|

**规则**：若只找到一�?�?另一侧必须写 `未找到（已搜：关键词…）`�?*禁止**留空或默认「同左」�?

### �?3 · 需求关键词覆盖

把轮 0 关键词逐条 Grep�?

| 关键�?| 命中路径（可多条�?| 与本需求关�?| 未命中时的结�?|
|--------|--------------------|--------------|----------------|
| | | 复用/扩展/新建/无关 | |

任一核心词「想当然有实现」但 Grep 无命�?�?�?**缺口**，不得写成已有能力�?

### �?4 · 对抗复核（防自嗨�?

换一轮提问（可新开子代理或自检清单）专门找漏：

- 「还有哪些站�?国家分支没打开文件？�?
- 「回�?校验/AB/草稿是否只搜了中文或只搜了英文？�?
- 「扩展点结论是否至少否定�?2 个候选路径？�?

输出：`## 对抗复核`：仍存疑点列表；无则写「本轮未发现额外轴，残留风险：…�?

---

## 证据标准（每条事实）

可接受：

- `path/to/File.ts` / `symbolName`（或行号区间�?
- Grep 模式 + 至少一条命中路�?
- 明确�?`未找到（已搜：…）`

不可接受�?

- 「应该在某某模块�?
- 「与现有逻辑类似�?
- 空单元格、复制粘贴「同上」跨国家/站点

---

## 门禁（写技术方案前�?

```text
PASS 仅当同时满足�?
1. 摸底文档存在且含：检索计划、四轮痕迹、路径级证据
2. 对比轴每一侧有证据或「未找到（已搜）�?
3. 需求关键词覆盖表无「假装已有�?
4. §不确定项 列出所有猜测（不得藏在正文当事实）
5. 用户显式回复含「摸底已核实」或勾选文档「人工核�?= 已核实�?
```

�?PASS �?**STOP**，禁�?`plan-workflow` / �?tech-design / 大范围实现�? 
用户可要求「带风险继续」——须在方案文首记�?*未核实条�?*�?

### AI 自检（提交摸底初稿时必须打印�?

```markdown
【摸底自检�?
- 检索计划：�?/ �?
- �? 入口链：�?/ �?
- �? 对比轴差分：�?/ 无（轴：…）
- �? 关键词覆盖：�?/ 无（未命中数：n�?
- �? 对抗复核：有 / �?
- 空单元格当「相同」：�?/ 否（必须为否�?
- 请用户核实：�?
```

自检任一项失�?�?先补摸底，不进入方案�?

---

## 与相近能�?

| 需�?| Skill |
|------|------|
| 功能/API/地区差分摸底 | `codebase-inventory` |
| **从代码提炼工程惯�?* | `codebase-conventions` |
| 业务规则/术语/UI | `domain-*-knowledge-capture` |

## 反模�?

- 一轮对话填完整张表
- 用架构常识补全未打开的文�?
- 把摸底写成实现方�?
- 人未核实就写 HOW 方案
