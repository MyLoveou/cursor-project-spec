---
description: "{DOMAIN_NAME} 域核心约束（模板 — 复制域包后改名并改 globs）"
globs: "{PATH_GLOBS}"
alwaysApply: false
---

# {DOMAIN_NAME} · 域核心

> 本文件来自 `domains/_template`。复制为真实域后：
> 1. 重命名为 `domain-<id>-core.mdc`
> 2. 将 `globs` 改为该域真实路径
> 3. 删除或改写下列占位条款

## 范围

- 仅当编辑匹配 `globs` 的文件时生效
- 不替代仓库根通用 Rule（`common-*`、`ai-execution` 等）

## 域不变量

1. {DOMAIN_INVARIANT_1}
2. {DOMAIN_INVARIANT_2}

## 禁止

- {DOMAIN_FORBIDDEN_1}
