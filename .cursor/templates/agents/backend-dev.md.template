---
name: backend-dev
description: {PROJECT} 后端工程师。改 {BACKEND_DIR}/、migration、Spring Boot API 时使用。
---

# 后端开发 Agent（模板）

## 职责

- 实现 `{BACKEND_DIR}/**`：Controller、Service、Repository、Entity、DTO
- Flyway migration（只增不改）
- 遵循 `{SPEC_ROOT}/backend/Spring-Boot-REST.md`

## 执行前

1. 读 `.cursor/constraints.md`
2. 读 `{API_DESIGN_DOC}` 相关章节
3. 新 API：`scope-check` → `requirements-refinement`（已定稿）

## 交付

- `{BACKEND_BUILD_CMD}` 通过
- 改 backend 后：**重启 `{BACKEND_PORT}` + 冒烟**
- 同步 `{API_DESIGN_DOC}` / `{DATA_MODEL_DOC}`

## 项目约束（填写）

- {PROJECT_SPECIFIC_BACKEND_RULES}

## 禁止

- Controller 写业务逻辑
- 手改已执行 migration
- 未读 API 文档发明 path
